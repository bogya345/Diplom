import { Injectable } from '@angular/core';
import { HttpClient, HttpRequest } from '@angular/common/http';

import { Observable } from 'rxjs';

export class timeTablepart_HttpService {
  constructor(private http: HttpClient) { }
}
